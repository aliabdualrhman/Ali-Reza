-- =============================================================================
-- 0090 — نوع طلب ثالث: التوصيل (طلب المندوب)
-- =============================================================================
-- **ملفٌّ من سطرٍ واحد، وحده، عمداً.**
--
-- القيمة الجديدة في نوعٍ معدود لا تُستعمل في المعاملة التي أضافتها:
--
--     unsafe use of new value "delivery" of enum type trip_kind
--
-- ومحرّر SQL في Supabase يشغّل الملف كلّه معاملةً واحدة. فلو جاءت هذه
-- الإضافة في رأس 0091 لسقط 0091 كلّه عند أول فهرسٍ أو شرطٍ يذكرها.
--
-- **شغّل هذا أولاً، ثم 0091.** والمواصفات في docs/features/delivery.md.
-- =============================================================================

alter type public.trip_kind add value if not exists 'delivery';

-- **لا فحص هنا.** حتى قراءة القائمة (`enum_range`) تلمس القيمة الجديدة
-- فتُسقط الملف بالخطأ نفسه — وقع هذا عند أول تطبيق. افحص بعده، وحده:
--
--     select enum_range(null::public.trip_kind);
